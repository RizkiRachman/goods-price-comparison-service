# Workspace template for PipelineRun
# Used to create a fresh workspace for each pipeline execution
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  generateName: workspace-
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

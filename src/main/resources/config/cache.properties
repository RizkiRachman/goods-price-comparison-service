# =============================================
# Cache Configuration
# =============================================

# Enable/disable caching
cache.enabled=true

# LLM Response Cache
cache.llm.max-size=1000
cache.llm.expire-hours=24

# Price Search Cache  
cache.price.max-size=500
cache.price.expire-hours=1

# Product Trend Cache
cache.trend.max-size=200
cache.trend.expire-hours=6

# =============================================
# CORS Configuration
# =============================================
cors.allowed-origin-patterns=${CORS_ALLOWED_ORIGINS:http://localhost:5173,https://aneh.biz.id}
cors.allowed-methods=GET,POST,PUT,PATCH,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# =============================================
# Rate Limiter Configuration
# =============================================

rate-limiter.enabled=true
rate-limiter.limit=60
rate-limiter.window-seconds=60

# Per-endpoint overrides (bracket-notation required for path keys)
# rate-limiter.endpoints[/api/v1/optimize/shopping].limit=10
# rate-limiter.endpoints[/api/v1/optimize/shopping].window-seconds=60

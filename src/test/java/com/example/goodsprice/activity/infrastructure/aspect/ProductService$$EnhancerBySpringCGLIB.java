package com.example.goodsprice.activity.infrastructure.aspect;

/**
 * Standalone top-level class simulating a Spring CGLIB proxy. Must NOT be a nested class
 * (getSimpleName() on nested classes with {@code $$} returns the portion after the last {@code $},
 * breaking proxy marker detection).
 */
@SuppressWarnings("checkstyle:TypeName")
class ProductService$$EnhancerBySpringCGLIB {}

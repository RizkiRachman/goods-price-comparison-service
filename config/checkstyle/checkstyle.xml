<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Focused on naming conventions and structural rules only.
  Formatting (indentation, line length, whitespace) is enforced by Spotless / google-java-format.
-->
<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <!-- Every source file must end with a newline -->
    <module name="NewlineAtEndOfFile"/>

    <!-- Enable @SuppressWarnings annotations to suppress checkstyle violations -->
    <module name="SuppressWarningsFilter"/>

    <module name="TreeWalker">
        <!-- Required to process @SuppressWarnings annotations -->
        <module name="SuppressWarningsHolder"/>

        <!-- ===== Naming ===== -->
        <module name="PackageName">
            <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
        </module>
        <module name="TypeName"/>
        <module name="MethodName"/>
        <module name="MemberName"/>
        <module name="ParameterName"/>
        <module name="LocalVariableName"/>
        <module name="ConstantName"/>
        <module name="CatchParameterName"/>
        <module name="LambdaParameterName"/>
        <module name="ClassTypeParameterName">
            <property name="format" value="^[A-Z][A-Z0-9]*(_[A-Z][A-Z0-9]*)*$|^[A-Z]$"/>
        </module>
        <module name="MethodTypeParameterName">
            <property name="format" value="^[A-Z]$"/>
        </module>

        <!-- ===== Imports ===== -->
        <module name="AvoidStarImport"/>
        <module name="IllegalImport"/> <!-- bans sun.* -->
        <module name="RedundantImport"/>
        <module name="UnusedImports"/>

        <!-- ===== Literals ===== -->
        <!-- Use L not l for long literals -->
        <module name="UpperEll"/>

        <!-- ===== Style ===== -->
        <!-- int[] x not int x[] -->
        <module name="ArrayTypeStyle"/>
        <!-- modifier order: public static final, not static public final -->
        <module name="ModifierOrder"/>
        <!-- no redundant modifiers on interface members -->
        <module name="RedundantModifier"/>

    </module>
</module>

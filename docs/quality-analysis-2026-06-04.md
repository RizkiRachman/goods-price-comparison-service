# Service Code Quality Analysis — 2026-06-04

**Task**: Analyze all service implementation files for code duplication, hardcoded strings, and extractable patterns.
**Method**: 3 parallel workstreams — PMD CPD, targeted grep, GitNexus cross-service analysis.
**Scope**: Domain services + persistence/web adapters (excluded tests, entities, DTOs, constants).

---

## Duplication

### PMD CPD
- **0 exact duplication blocks** at 30+ token minimum
- Note: `pmd:cpd` goal is not configured in pom.xml — results from manual CLI invocation

### Pattern-Level Duplication (CPD cannot detect)
| Pattern | Files | Occurrences |
|---------|-------|-------------|
| `findById → Objects.isNull → NotFoundException.factory` | StoreService, ProductService, PriceService | 3 replicas |
| `"Unbounded findAll() called..."` log string | ProductRepositoryAdapter:69, PriceRepositoryAdapter:60 | 2 replicas |

---

## Hardcoded Strings & Magic Values

### CRITICAL → HIGH (reclassified by review)
| Issue | Files | Fix |
|-------|-------|-----|
| `"ACTIVE"` status strings | CategoryService:44, UnitService:44, AlertService:41 | Use `EntityStatus.ACTIVE` or new enum constant |

### HIGH
| Issue | Files | Fix |
|-------|-------|-----|
| Cache literals ignoring constants | StoreRepositoryAdapter, FeedbackQuestionRepositoryAdapter | Use `CacheConfiguration.STORES_CACHE`, `FEEDBACK_QUESTIONS_CACHE` |
| Missing cache constants | UnitRepositoryAdapter, ShoppingOptimizer, BillSplitService, LlmService | Add to `CacheConfiguration` |
| Default page size `20` hardcoded | PriceWebAdapter:84, PaginationUtils:43 | Use `AppConstants.DEFAULT_PAGE_SIZE` |

### HIGH → MEDIUM (reclassified)
| Issue | Files | Fix |
|-------|-------|-----|
| `"asc"`/`"desc"` sort direction literals | PriceWebAdapter, AbstractCrudWebAdapter, PaginationHelper, PriceRepositoryAdapter | Use `SortConstants.ASC`/`DESC` |

### MEDIUM
| Issue | Files | Fix |
|-------|-------|-----|
| Missing error message constants | AdminService (3 msgs), PriceWebAdapter (1 msg) | Add to `ErrorMessageConstants` |
| Specification field name literals | StoreRepositoryAdapter, UnitRepositoryAdapter, ProductSpecification | Use `EntityConstants.NAME/STATUS/ID/CATEGORY` |
| Missing EntityConstants entries | `brand`, `location`, `chain`, `address`, `symbol`, `type` | Add to `EntityConstants` |

### LOW → MEDIUM (reclassified)
| Issue | Files | Fix |
|-------|-------|-----|
| Missing `@CacheEvict` on deleteById | UnitRepositoryAdapter, CategoryRepositoryAdapter, FeedbackQuestionRepositoryAdapter | Add cache eviction |

### LOW
| Issue | Files | Fix |
|-------|-------|-----|
| `"SHA-256"` algorithm literal | LlmService:47 | Extract to constant |

---

## Pattern Analysis

| Pattern | Adoption | Notes |
|---------|----------|-------|
| `AbstractGenericService` | 4/12 services (33%) | Store, Product, Price, Receipt, Alert, Shopping, Admin, System don't use it |
| `AbstractRepositoryAdapter` | 8/8 adapters (100%) | Full adoption |
| `SpecificationBuilder` | 3/5 adapters (60%) | ActivityLog uses raw CB; ProductSpecification is separate class |
| Cache constant references | 1/4 adapters (25%) | Only Category uses `CacheConfiguration.XXX_CACHE` |
| `@CacheEvict` on deleteById | 1/4 adapters (25%) | Only Store has it |
| Web list response pattern | 7/7 adapters (100%) | Identical `setData`/`setPagination` pattern |
| Cross-service boundaries | Clean | All inter-service calls through port interfaces |

---

## Key Recommendations (Priority-Ordered)

1. **Immediate (High ROI, Low Risk)**: Use existing constants — `CacheConfiguration.STORES_CACHE`, `AppConstants.DEFAULT_PAGE_SIZE`, `SortConstants.ASC/DESC`, `EntityConstants.*` in specs
2. **Short-term**: Add missing constants — `"units"`, `"shopping-optimization"`, `"bill-splits"`, `"llm-responses"` to `CacheConfiguration`; add `"brand"`, `"location"`, etc. to `EntityConstants`
3. **Medium-term**: Add `@CacheEvict` on deleteById to Unit, Category, FeedbackQuestion adapters
4. **Long-term**: Evaluate extending `AbstractGenericService` adoption to Store, Product, Price services

---

*Generated by orchestration task `service-code-quality-analysis`*
